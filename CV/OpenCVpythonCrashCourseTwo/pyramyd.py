import cv2import numpy as np""" what is the use of gaussian and laplacian pyramid ? it'll help us to blend and reconstruction the images"""""" it reduces the resolution of an image """""" as we go down the image the resolution of the images get reduced... as we go down the resolution gets higher """# the value of lena_img variable is just and metrics or array the pixelslena_img = cv2.imread('Photos/Lena.png', 0)# cv2.imshow('lena_img', lena_img)### lowerResolu_1 = cv2.pyrDown(lena_img)# lowerResolu_2 = cv2.pyrDown(lowerResolu_1)## higher_Resolu_1 = cv2.pyrUp(lowerResolu_1)### cv2.imshow('lowerResolu_1', lowerResolu_1)# cv2.imshow('lowerResolu_2', lowerResolu_2)# cv2.imshow('higher_Resolu_1', higher_Resolu_1)  # as we go up the resolution gets increased# ######################################################################################## #######################################################################################""" we can use for loop to make it simple"""""" not clear how he does it """# first copy the image pixelscopied_layer = lena_img.copy()box = [copied_layer]for i in range(6):    copied_layer = cv2.pyrDown(copied_layer)    box.append(copied_layer)    cv2.imshow(str(i), copied_layer)cv2.waitKey(0)cv2.destroyAllWindows()