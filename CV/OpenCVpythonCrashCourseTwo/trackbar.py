import cv2import numpy as np""" pixels are so small that human eye cannot differentiate. """## def nothing(x):#     print(x)### black_img = np.zeros((300, 512, 3), np.uint8)# cv2.namedWindow('black_img')## cv2.createTrackbar('B', 'black_img', 0, 255, nothing)# cv2.createTrackbar('G', 'black_img', 0, 255, nothing)# cv2.createTrackbar('R', 'black_img', 0, 255, nothing)### switch = '0 : OFF\n 1 : ON'# cv2.createTrackbar(switch, 'black_img', 0, 1, nothing)#### while(1):#     cv2.imshow('black_img', black_img)#     if cv2.waitKey(1) & 0xFF == 27:#         break##     b = cv2.getTrackbarPos('B', 'black_img')#     g = cv2.getTrackbarPos('G', 'black_img')#     r = cv2.getTrackbarPos('R', 'black_img')#     switcher = cv2.getTrackbarPos(switch, 'black_img')##     if switcher == 0:#         black_img[:] = 0#     else:#         black_img[:] = [b, g, r]## cv2.destroyAllWindows()# ===============================================================================================""" The above code will turn the black_img when the switcher is 0 else will show us a BGR img """# ===============================================================================================